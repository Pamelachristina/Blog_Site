<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <title>{{#if slug}}Edit Entry{{else}}Add New Entry{{/if}} - SparkBlog</title>
    <link href='https://fonts.googleapis.com/css?family=Noto+Serif:400,400italic,700|Noto+Sans:700,400' rel='stylesheet' type='text/css'>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="/css/normalize.css">
    <link rel="stylesheet" href="/css/site.css">
</head>
<body>
<header>
    <div class="container">
        <div class="site-header">
            <a class="logo" href="/">SparkBlog</a>
            <a class="new-entry button button-round" href="/new"><i class="material-icons">create</i></a>
        </div>
    </div>
</header>
<section>
    <div class="container">
        <div class="edit-entry">
            <h2>{{#if slug}}Edit Entry{{else}}Add New Entry{{/if}}</h2>

            <!-- Edit or Add Form -->
            <form action="{{#if slug}}/entry/{{slug}}/edit{{else}}/add{{/if}}" method="post">
                <label for="title">Title</label>
                <input type="text" name="title" id="title" value="{{title}}" required><br>

                <label for="body">Entry</label>
                <textarea rows="5" name="body" id="body" required>{{body}}</textarea>

                <input type="submit" value="Save Entry" class="button">
                <a href="/" class="button button-secondary">Cancel</a>
            </form>

            <!-- Delete Button -->
            {{#if slug}}
                <form action="/entry/{{slug}}/delete" method="post" onsubmit="return confirm('Are you sure you want to delete this entry?');">
                    <input type="submit" value="Delete Entry" class="button button-danger">
                </form>
            {{/if}}
        </div>
    </div>
</section>
<footer>
    <div>
        <a href="#">Contact Us</a>&nbsp;&nbsp;&bull;&nbsp;&nbsp;
        <a href="#">Terms</a>
    </div>
</footer>
</body>
</html>



